// Button component that can be used to advance pages, this component wasn't used as we used <button> in JSX instead

import React from "react";

function AdvanceButton() {
  return <button className="advance-button">{text}</button>;
}

export default AdvanceButton;
